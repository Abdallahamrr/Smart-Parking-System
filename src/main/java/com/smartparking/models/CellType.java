package com.smartparking.models;

public enum CellType {
    ROAD,
    PARKING_SPOT,
    RAMP_UP,
    RAMP_DOWN,
    RAMP_BOTH,   // floor has both up and down
    ELEVATOR,
    WALL,
    INTERSECTION
}